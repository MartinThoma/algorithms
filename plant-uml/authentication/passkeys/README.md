# Minimal passkey example

## Installation

```bash
pip install flask fido2 flask-cors
```

## Usage

```bash
# Start backend
flask run --host=localhost
```

Now open your browser and navigate to `http://localhost:5000/`.